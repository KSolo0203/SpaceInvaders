#include "SpaceInvaders.h"
#include <iostream>
#include <thread>
#include "timer.h"

int main()
{
    // SAOptions options = SAOptions();
    // SAGameEngine game_engine = SAGameEngine(options);

    int timer = 0;
    while (true) {
        // game_engine();

        // std::cout << "Hi! Timer is: " << timer << "\r";
        // timer++;
        // std::this_thread::sleep_for(tick);
        break;
    }

    int level = 0;
    Ship player;
    player.fire();
}
